{
    "name": "ecoreng/module-loader-middleware",
    "license": "MIT",
    "type": "library",
    "description": "Loads and inits processes from a bootstrap file allowing to configure the slim app from within an encapsulated module",
    "autoload": {
        "psr-4": { 
            "ecoreng\\Module\\": "src"
            }
    },
    "require": {
        "php": ">=5.4",
				"slim/slim": ">=2.4.0"
    },
		"require-dev": {
        "phpunit/phpunit": "~4.3"
    }
}
